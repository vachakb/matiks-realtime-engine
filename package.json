{
  "name": "@matiks/realtime-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Real-time duel engine for Matiks: a zero-dependency TypeScript core — client-side prediction + server reconciliation, bot/cadence detection, pluggable JSON/msgpack codec — over a swappable Transport interface, plus a measured off-thread AES-decrypt Nitro (JSI) module and a runnable demo.",
  "engines": { "node": ">=23.6" },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "license": "UNLICENSED"
}
